[project]
name = "three-amigos-offline"
version = "1.3.0"
description = "Offline triple-model speech-to-text assistant for macOS (MLX Whisper tiers)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
